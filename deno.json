{
  "imports": {
    "@sigmasd/gtk": "jsr:@sigmasd/gtk@^0.65.0"
  },
  "tasks": {
    "compile": "deno compile --allow-ffi --output screen-light main.ts",
    "install": "deno task compile && deno run -A jsr:@sigmasd/install-app install screen-light"
  }
}
